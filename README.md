```
mkdir ~/src
cd ~/src
git clone git@github.com:josler/promptly.git
cd promptly
bundle
echo 'export PATH=$HOME/src/promptly/bin:$PATH' >> ~/.profile
# OR
echo 'export PATH=$HOME/src/promptly/bin:$PATH' >> ~/.zshrc
```
